# Desktop MCP Bridge

A secure, permission-aware Windows desktop bridge that exposes local desktop, filesystem, process, and shell capabilities to ChatGPT through the Model Context Protocol (MCP).

> Development is in progress on a feature branch. Do not expose an unrestricted instance to the public internet.
